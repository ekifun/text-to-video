# Stage 1: Build
FROM golang:1.21 AS builder

WORKDIR /app

# Copy go mod files and tidy
COPY go.mod go.sum ./
RUN go mod tidy

# Copy source files
COPY . .

# Build the consumer binary
RUN CGO_ENABLED=0 GOOS=linux go build -o consumer ./kafka/consumer.go

# Stage 2: Minimal image
FROM alpine:latest

WORKDIR /root/

COPY --from=builder /app/consumer .

# Run the consumer
ENTRYPOINT ["./consumer"]
